# Supabase (required for database, auth, and realtime when implemented)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Public base URL for user-uploaded media served from object storage
VITE_MEDIA_PUBLIC_BASE_URL=

# Server-side media upload signing endpoint (implementation TBD)
# The browser must never receive R2 or other object-storage credentials.
VITE_MEDIA_UPLOAD_API_URL=
