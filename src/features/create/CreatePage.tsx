import { FeaturePlaceholderPage } from '@/features/shared/FeaturePlaceholderPage';

export function CreatePage() {
  return (
    <FeaturePlaceholderPage
      title="Create"
      description="Lightweight composer for text, images, galleries, and short video. Drafts will persist locally via IndexedDB before publish is implemented."
    />
  );
}
