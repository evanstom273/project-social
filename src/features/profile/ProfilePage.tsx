import { FeaturePlaceholderPage } from '@/features/shared/FeaturePlaceholderPage';

export function ProfilePage() {
  return (
    <FeaturePlaceholderPage
      title="Profile"
      description="Creator shelf, recent posts, and follow controls. Profiles should foreground projects rather than vanity metrics."
    />
  );
}
